# Дизайн-система MultiCallculator

Единый источник визуальных правил проекта. Перед UI-задачами использовать этот файл вместе со страницей-витриной `/design-system/`.

## Базовые токены

### Цвета
- `#111827` — основной тёмный цвет: шапка, основные action-кнопки, активный текст на светлом фоне.
- `#1f2933` — основной цвет текста.
- `#667085` — вторичный/поясняющий текст.
- `#cbd5e1` — вторичный текст на тёмной шапке.
- `#f4f6f8` — фон приложения.
- `#ffffff` — фон карточек и полей.
- `#f9fafb` — мягкий фон текстовых/служебных блоков.
- `#eef2ff` — фон информационных pill-элементов.
- `#d0d5dd` — граница полей формы.
- `#eaecf0` — мягкие разделители и границы.

Не вводить новые близкие серые и синие оттенки без необходимости. Сначала использовать значения выше.

### Типографика
- Шрифт: `Arial, sans-serif`.
- Заголовок страницы внутри калькулятора: `26px`.
- Заголовок секции: `20px`.
- Подзаголовок/номер секции: `16px`.
- Основной текст полей: `15px`.
- Вспомогательный текст, таблицы, pill: `14px`.
- Подпись в шапке: `13px`.
- Крупный итог: `30px`, `font-weight: 800`.

### Радиусы
- Карточки: `16px`.
- Поля ввода/select/textarea: `10px`.
- Основные полноширинные кнопки: `12px`.
- Pill/tab-кнопки: `999px`.
- Текстовые служебные блоки: `12px`.

### Отступы
- Основной внутренний отступ страницы калькулятора: `24px`.
- Карточка: `22px`.
- Между колонками карточек: `20px`.
- Сетки форм: `12px`.
- Label: `14px 0 6px`.
- Основная кнопка: `13px` по вертикали.

### Тени
- Карточка: `0 8px 24px rgba(0,0,0,0.08)`.
- Шапка: `0 6px 18px rgba(0,0,0,0.18)`.

## Компоненты

### Шапка
Тёмный фон `#111827`, белый основной текст, подпись `#cbd5e1`. Вкладки — pill-кнопки с радиусом `999px`.

### Карточка
Белый фон, радиус `16px`, padding `22px`, мягкая тень.

### Поля формы
Белый фон, граница `1px solid #d0d5dd`, радиус `10px`, padding `11px`, размер текста `15px`.

### Основная кнопка
Полноширинная, фон `#111827`, белый текст, радиус `12px`, размер текста `16px`.

### Pill / статус
Фон `#eef2ff`, радиус `999px`, padding `7px 10px`, текст `14px`.

### Служебный текстовый блок
Фон `#f9fafb`, граница `1px solid #eaecf0`, радиус `12px`, padding `14px`, текст `14px`, line-height `1.45`.

## Найденные расхождения и единый эталон

1. В проекте одновременно используются радиусы `10px`, `12px`, `16px` и `999px`. Это не считать ошибкой само по себе: закрепить их по ролям компонентов, а не смешивать внутри одного типа компонента.
2. Для кнопок встречаются pill-вкладки и прямоугольные action-кнопки. Эталон: `999px` только для tabs/pills; `12px` для основных action-кнопок.
3. В светлых фонах используются `#f4f6f8`, `#f9fafb`, `#eef2ff`. Эталон: `#f4f6f8` — фон страницы, `#f9fafb` — служебные блоки, `#eef2ff` — информационные pill.
4. Основной тёмный цвет интерфейса — `#111827`. Не вводить другие «почти такие же» тёмно-синие/серые без отдельного решения.

## Правила для новых экранов
- Новые UI-блоки сначала собирать из существующих токенов и компонентов.
- Не придумывать новый шрифт, палитру, радиусы и систему отступов без явной причины.
- При расхождении между новым экраном и витриной `/design-system/` источником считать эту дизайн-систему.
- После осознанного изменения стиля обновлять и `DESIGN.md`, и витрину `/design-system/`.
